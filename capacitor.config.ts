import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.factualai',
  appName: 'FactualAi',
  webDir: '.next',           // Next.js outputs here
};

export default config;
